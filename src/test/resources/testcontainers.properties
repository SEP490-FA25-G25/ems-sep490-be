# Testcontainers configuration
# Enable container reuse across test runs for faster execution
testcontainers.reuse.enable=true

# Checks for new versions of Docker images
# Set to false to speed up tests (use cached images)
checks.disable=true
